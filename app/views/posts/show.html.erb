
<a href="#" class="js-next" data-id="<%=@post.id%>">Next...</a>
<h1 class="postTitle"><%= @post.title %></h1>
<p class="postBody"><%= @post.description %></p>

<script type="text/javascript" charset="utf-8">
$(function () {
  $(".js-next").on("click", function() {
    var nextId = parseInt($(this).data("id")) + 1;
    $.getJSON("/posts/" + nextId + "/post_data", function(response) {
      $(".authorName").html(response["author"]["name"]);
      $(".postTitle").html(response["title"]);
      $(".postBody").html(response["description"]);
      // re-set the id to current on the link
      $(".js-next").attr("data-id", resposnse["id"]);
    });
  });
});
</script>
